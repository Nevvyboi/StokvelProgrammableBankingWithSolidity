title = "stokvel talk"

[extend]
useDefault = true

[allowlist]
description = "build output and dependencies"
paths = [
  '''node_modules''',
  '''\.next''',
  '''contracts/lib''',
  '''contracts/out''',
  '''contracts/cache''',
  '''package-lock\.json''',
  '''stage/public/fallback/snapshot\.json''',
  '''cards/out/''',
]
# cards/out is gitignored and is where the card generator keeps the throwaway testnet keys on purpose
